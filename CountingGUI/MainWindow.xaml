<Window x:Class="CountingGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CountingGUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="850" Width="1000">
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock x:Name="SymbolsCountTextTextBlock" Text="Количество символов: "/>
            <TextBlock x:Name="SymbolsCountTextBlock" Text="123" Grid.Column="1"/>
            <Button x:Name="test" Content="Обновить" Grid.Row="1" Click="test_Click" IsDefault="true"/>
        </StackPanel>
        <Grid x:Name="DynamicGrid" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
        </Grid>
    </Grid>
</Window>
